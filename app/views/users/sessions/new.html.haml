%h2
  Connexion
%p
  Utilisez le formulaire ci-dessous, pour vous identifier. Veuillez nous indiquer l'adresse e-mail avec laquelle vous avez créé votre compte.
%p
  Si vous n'avez pas encore de compte, vous pouvez #{ link_to "créer un compte ici", new_user_registration_path }.
  
- if devise_mapping.authenticatable?
  - form_for resource_name, resource, :url => session_path(resource_name), :html => { :class => "form-horizontal" } do |f|

    - if flash[:alert].present? and flash[:alert]
      - error_class = "error"
      .alert.alert-error
        %a.close{ "data-dismiss" => "alert" } ×
        != flash[:alert]
    - else
      - error_class = ""

    .control-group{ :class => error_class }
      != f.label :email, :"Identifiant :", :class => "control-label"
      .controls
        != f.text_field :email

    .control-group{ :class => error_class }
      != f.label :password, :"Mot de passe :", :class => "control-label"
      .controls
        != f.password_field :password

    - if devise_mapping.rememberable?
      .margin1top
        .control-group
          .controls
            != f.check_box :remember_me, :class => "pull-left"
            != f.label :remember_me, "Restez connecté à mon compte (session de 2 jours)", :class => "control-label margin2left"

    .form-actions
      != f.submit "VALIDER", :class => "btn pull-left"
      %a.btn.margin1left{ :href => "javascript:history.back();"}
        %span
          RETOUR