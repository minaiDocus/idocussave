#customer
  .row-fluid
    .span12.box
      %h3 Edition du client : #{link_to @user.info, account_organization_customer_path(@user)}
  .rwo-fluid
    .span12.box
      = render partial: 'form'
