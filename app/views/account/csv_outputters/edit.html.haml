#csv_outputters.edit
  .row-fluid
    .span12.box
      %h3 Edition du format de sortie CSV du client: #{link_to @customer, account_organization_customer_path(@organization, @customer, tab: 'csv_outputter')}

  .row-fluid
    .span12.box
      = render partial: 'form'
