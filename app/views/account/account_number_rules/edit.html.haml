#account_number_rule.edit.derivationRight
  .card
    .card-header
      %h3 
        Edition de la règle : 
        %span.text-info.fs-origin= @account_number_rule.name
    = render partial: 'form'
