%h3 Filtres

%br
= form_tag account_organizations_path, method: :get, class: 'form form-vertical margin0' do
  = hidden_field_tag :direction, sort_direction
  = hidden_field_tag :sort, sort_column
  = hidden_field_tag :per_page, per_page
  = label_tag 'organization_contains[name]', t('mongoid.models.organization.attributes.name')
  = text_field_tag 'organization_contains[name]', (params[:organization_contains][:name] rescue ''), class: 'span12'
  = label_tag 'organization_contains[description]', t('mongoid.models.organization.attributes.description')
  = text_field_tag 'organization_contains[description]', (params[:organization_contains][:description] rescue ''), class: 'span12'
  %br
  = submit_tag t('filters.action'), class: 'btn btn-primary'
  = link_to t('filters.reset'), account_organizations_path, class: 'btn'
