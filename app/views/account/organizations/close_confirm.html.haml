#organizations.organization_close_confirm
  .row-fluid
    .span12.box.aligncenter
      .warning.margin1top.margin2bottom
        Voulez-vous réellement clôturer l'organisation' "#{@organization.name}" ?
      = link_to 'Oui', deactivate_account_organization_path(@organization), method: 'patch', class: 'btn'
      = link_to 'Non', account_organization_path(@organization), class: 'btn'
