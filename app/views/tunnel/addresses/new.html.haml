.leftcolon.margin4top.margin4bottom
  != render :partial => '/shared/error_messages', :locals => {:object => @address}
    
  .tunnelcase
    %ul.progress
      %li.first
        %span
          Produits
      %li.second.current
        %span
          Options
      %li.third
        %span
          Paiement


    - form_for [:tunnel, @address], :html => {:method => :post, :class => 'clearfix'} do |f|
      %h2.margin2top
        Ajouter une nouvelle adresse :
      - if current_user.addresses.empty?
        %p.margin1top
          Votre carnet d'adresses ne contient aucune adresse, veuillez renseigner le formulaire ci-dessous afin de poursuive votre achat.

      != render :partial => 'form', :locals => {:f => f}
