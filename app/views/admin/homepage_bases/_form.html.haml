= semantic_form_for [:admin, @homepage_base] do |f|
  = f.inputs 'Informations' do
    = f.input :name, label: 'Nom'
    = f.input :style, as: :text, label: 'Style'
    = f.input :content, as: :text, label: 'Contenu'
    = f.input :meta_description, as: :text, label: 'Méta-description'
  = f.buttons do
    = f.commit_button
    = content_tag(:li, (link_to t('active_admin.cancel'), admin_reminder_emails_path), class: 'cancel')