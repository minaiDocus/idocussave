.row-fluid
  .span12.well
    %h2 Informations
    %br
      = simple_form_for [:admin, @product_option], html: { class: 'form form-horizontal' } do |f|
        .row-fluid
          .span4
            = f.input :title, label: 'Titre'
            = f.input :position, as: 'integer'
            = f.input :require_addresses, as: 'boolean', label: 'Adresse requise ?'
            = f.input :price_in_cents_wo_vat, as: 'integer', label: 'Prix (en centimes)'
            = f.input :duration, as: 'integer', label: "Durée de l'abonnement ( en nombre de mois, laisser à 1 pour normal ) ?"
            = f.input :quantity, as: 'integer', label: 'Quantité'
            = f.input :product_group, as: 'select', collection: ProductGroup.asc(:name).map { |e| [e.name,e.id] }, include_blank: false
          .span4
            = f.input :description, as: 'text', input_html: { class: 'xxlarge' }
        .row-fluid
          .span12
            .form-actions
              = f.submit t('actions.submit'), class: 'btn'
          