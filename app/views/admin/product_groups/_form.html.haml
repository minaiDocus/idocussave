.group
  != f.label :name, "Name * (nom du produit dans l'administration)", :class => :label
  != f.text_field :name, :class => 'text_field'

.group
  != f.label :title, "Title * (nom du produit dans le tunnel)", :class => :label
  != f.text_field :title, :class => 'text_field'
  
.group
  != f.label :description, "Déscription", :class => :label
  != f.text_field :description, :class => 'text_field'
  
.group
  != f.label :position, "Position", :class => :label
  != f.text_field :position, :class => 'text_field'
  
  
.group
  != f.label :is_option_dependent, "Bouton radio ? (sinon case à cocher)", :class => :label
  != f.check_box :is_option_dependent, :class => 'check_box'
  
.group
  != f.label :supergroup_id, "Sélectionner un super groupe", :class => :label
  != f.select(:product_supergroup_id, ([["aucun",""]] + ProductGroup.all.entries.map{|g| [g.name,g.id]}))
  
.group
  != f.label :require_id, "Sélectionner un groupe pour la dépendance", :class => :label
  != f.select(:product_require_id, ([["aucun",""]] + ProductGroup.all.entries.map{|g| [g.name,g.id]}))
  
.group
  != f.label :product_id, "Sélectionner un produit", :class => :label
  != f.collection_select :product_id, Product.find(:all), :id, :title, :prompt => "Sélectionner un produit"
  
.group.navform.wat-cf
  %button.button{:type => 'submit'}
    != image_tag("web-app-theme/tick.png", :alt => "Save")
    Save
  != link_to image_tag("web-app-theme/cross.png", :alt => "Cancel") + "Cancel", admin_products_path, :class => "button"

.group
  %em
    %b *
    mandatory fields.
