= simple_nested_form_for [:admin, @news], html: { class: 'form form-horizontal padding1top margin0' } do |f|
  = f.input :title
  = f.input :target_audience, collection: news_target_audience_options, include_blank: false
  = f.input :url, input_html: { class: 'span12' }
  = f.input :body, input_html: { rows: 7, class: 'span12' }, hint: raw("Formattage (gras, italic, etc...) : #{link_to 'http://commonmark.org/help/', 'http://commonmark.org/help/', target: '_blank'}")
  .form-actions.margin0bottom
    = f.submit t('actions.submit'), class: 'btn btn-primary'
    = link_to t('actions.cancel'), admin_news_index_path, class: 'btn'
