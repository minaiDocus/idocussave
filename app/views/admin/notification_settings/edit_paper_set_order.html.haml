#notification_settings.edit_error
  .card
    .card-header
      %h3 Edition du paramétrage des notifications pour les commandes de Kit envoi courrier

    = form_tag update_paper_set_order_admin_notification_settings_path, class: 'form form-horizontal margin0 padding1top' do
      .card-body
        .form-group.clearfix
          .label-section
            = label_tag 'notification[to]', 'Notifier à', class: 'control-label'
          .control-section
            = text_field_tag 'notification[to]', !Settings.first.notify_paper_set_order_to ? '' : Settings.first.notify_paper_set_order_to.join(', '), class: 'form-control w-25'
            %span.help-block Séparer les valeurs par une virgule. La deuxième et les suivantes seront en copie.
      .card-footer    
        .form-actions
          = submit_tag t('actions.submit'), class: 'btn btn-primary'
          = link_to t('actions.cancel'), admin_notification_settings_path, class: 'btn btn-light'
