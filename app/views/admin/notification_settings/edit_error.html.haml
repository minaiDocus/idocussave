#notification_settings.edit_error
  .row
    .col-12
      .panel
        %h3 Edition du paramétrage des notifications pour les erreurs

  .row
    .col-12
      .panel
        = form_tag update_error_admin_notification_settings_path, class: 'form form-horizontal margin0 padding1top' do
          .form-group
            = label_tag 'notification[to]', 'Notifier à', class: 'control-label'
            = text_field_tag 'notification[to]', !Settings.first.notify_errors_to ? '' : Settings.first.notify_errors_to.join(', '), class: 'form-control w-25'
            %span.help-block Séparer les valeurs par une virgule. La deuxième et les suivantes seront en copie.
          .form-actions.mt-3
            = submit_tag t('actions.submit'), class: 'btn btn-primary'
            = link_to t('actions.cancel'), admin_notification_settings_path, class: 'btn btn-outline-secondary'
