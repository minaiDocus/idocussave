.label{ style: 'padding:3px 5px 3px' }
  Abonnement en cours

- subscription = @user.scan_subscriptions.desc(:created_at).first
- if subscription && @user.active
  %table.table.table-striped.table-bordered.table-condensed
    %tbody
      - period = subscription.periods.desc(:created_at).first
      - if period
        - period.product_option_orders.asc([:group_position,:position]).each do |option|
          %tr
            %td= option.group_title
            %td= option.title
            %td.price= format_price_00(option.price_in_cents_wo_vat) + ' €'
        %tr
          %td Dépassement
          %td
          %td.price= format_price_00(period.price_in_cents_of_total_excess) + ' €' rescue ''
        %tr
          %th.total Total
          %td
          %td.price= format_price_00(period.total_price_in_cents_wo_vat) + ' €' rescue '0,00 €'
