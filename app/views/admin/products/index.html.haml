#products
  = render partial: 'navigation'

  .row-fluid
    .span9
      .row-fluid
        .span12.well
          .content
          .inner
            %h3.title Preview
            = render :partial => "product", :collection => @products
      .row-fluid
        .span12.well
          %h2 Produits
          %table.table.table-condensed
            %thead
              %tr
                %th.first= t('mongoid.models.product.attributes.position')
                %th.first= t('mongoid.models.product.attributes.title')
                %th.first= t('mongoid.models.product.attributes.price_in_cents_wo_vat')
                %th.first= t('actions.name')
            %tbody
              - @products.each do |product|
                %tr
                  %td.first= product.position
                  %td.first= product.title
                  %td.price.first= format_price_00(product.price_in_cents_wo_vat) + ' €'
                  %td.first
                    = link_to icon_edit, edit_admin_product_path(product)
                    = link_to icon_destroy, admin_product_path(product), data: { confirm: 'Voulez vous vraiment suprimer ce produit ?' }, method: 'delete'

      .row-fluid
        .span12.well
          %h2 Options
          %table.table.table-condensed
            %thead
              %tr
                %th.first= t('mongoid.models.product_option.attributes.position')
                %th.first= t('mongoid.models.product_option.attributes.name')
                %th.first= t('mongoid.models.product_option.attributes.title')
                %th.first= t('mongoid.models.product_option.attributes.price_in_cents_wo_vat')
                %th.first= t('mongoid.models.product_option.group')
                %th.first= t('actions.name')
            %tbody
              - @product_options.each do |product_option|
                %tr
                  %td.first= product_option.position
                  %td.first= product_option.name
                  %td.first= product_option.title
                  %td.price.first= format_price_00(product_option.price_in_cents_wo_vat) + ' €'
                  %td.first= product_option.product_group.try(:name)
                  %td.first
                    = link_to icon_edit, edit_admin_product_option_path(product_option)
                    = link_to icon_destroy, admin_product_option_path(product_option), data: { confirm: 'Voulez vous vraiment suprimer cette option ?' }, method: 'delete'

      .row-fluid
        .span12.well
          %h2 Groupes
          %table.table.table-condensed
            %thead
              %tr
                %th.first= t('mongoid.models.product_group.attributes.position')
                %th.first= t('mongoid.models.product_group.attributes.name')
                %th.first= t('mongoid.models.product_group.attributes.title')
                %th.first= t('mongoid.models.product.name').pluralize
                %th.first Nombre d'option(s)
                %th.first= t('actions.name')
            %tbody
              - @product_groups.each do |product_group|
                %tr
                  %td.first= product_group.position
                  %td.first= product_group.name
                  %td.first= product_group.title
                  %td.first= product_group.products.map { |e| e.title }.join(', ')
                  %td.first= product_group.product_options.count
                  %td.first
                    = link_to icon_edit, edit_admin_product_group_path(product_group)
                    = link_to icon_destroy, admin_product_group_path(product_group), data: { confirm: 'Voulez vous vraiment suprimer ce groupe ?' }, method: 'delete'

    = render partial: 'sidebar'