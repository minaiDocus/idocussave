# -*- encoding : UTF-8 -*-
require 'spec_helper'

describe SgiApiServices::GroupDocument do
  before(:all) do
      Timecop.freeze(Time.local(2020,06,12,0,1,0))
  end

  after(:all) do
    Timecop.return
  end

  describe '' do
    it ' ' do
      
    end
  end
end